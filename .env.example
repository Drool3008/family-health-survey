# Google Apps Script web app URL that appends rows to your Sheet.
# Deploy the script in google-apps-script.gs as a Web app, then paste its /exec URL here.
# Treat this URL as a secret (anyone with it can write to the sheet).
SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/XXXXXXXX/exec
